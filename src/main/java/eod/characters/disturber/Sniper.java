package eod.characters.disturber;

import eod.Character;
import eod.Player;

public class Sniper extends Character {
    public Sniper(Player player, int x, int y, int range) {
        super(player, x, y, range);
    }
}
