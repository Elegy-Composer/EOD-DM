package eod.characters.assaulter;

import eod.Character;
import eod.Player;

public class Shooter extends Character {
    public Shooter(Player player, int x, int y, int range) {
        super(player, x, y, range);
    }
}
